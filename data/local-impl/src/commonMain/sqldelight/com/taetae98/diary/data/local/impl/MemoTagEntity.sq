CREATE TABLE MemoTagEntity(
    memoId TEXT NOT NULL,
    tagId TEXT NOT NULL,

    FOREIGN KEY (memoId) REFERENCES MemoEntity(id),
    FOREIGN KEY (tagId) REFERENCES TagEntity(id),
    PRIMARY KEY (memoId, tagId)
);

upsert:
INSERT OR IGNORE INTO MemoTagEntity VALUES ?;